import 'package:flutter/material.dart';

class RetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Retail"),
      ),
      body: Center(
        child: Text("Retail Screen"),
      ),
    );
  }
}