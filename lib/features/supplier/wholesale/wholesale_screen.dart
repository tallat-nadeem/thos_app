import 'package:flutter/material.dart';

class WholesaleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Wholesale"),
      ),
      body: Center(
        child: Text("Wholesale Screen"),
      ),
    );
  }
}