import 'package:flutter/material.dart';

class ThosMarketScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("THOS Market"),
      ),
      body: Center(
        child: Text("THOS Market Screen"),
      ),
    );
  }
}